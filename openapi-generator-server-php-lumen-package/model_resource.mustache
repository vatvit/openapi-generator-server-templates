<?php

declare(strict_types=1);

/**
 * {{appName}}
 * Laravel Resource - Auto-generated by OpenAPI Generator
 *
 * @version {{appVersion}}
 * @link https://openapi-generator.tech
 */

namespace {{invokerPackage}}\Http\Resources;

use Illuminate\Http\Resources\Json\JsonResource;
{{#models}}
{{#model}}

/**
 * {{classname}}Resource
 *
 * Laravel Resource for {{classname}} response transformation
 * Enforces response structure from OpenAPI schema
 *
 * @category Resource
 * @package  {{invokerPackage}}\Http\Resources
 */
class {{classname}}Resource extends JsonResource
{
    /**
     * Transform the resource into an array.
     *
     * Structure enforced by OpenAPI specification
     *
     * @param  \Illuminate\Http\Request  $request
     * @return array<string, mixed>
     */
    public function toArray($request): array
    {
        return [
{{#vars}}
            '{{baseName}}' => $this->{{name}},
{{/vars}}
        ];
    }

    /**
     * Customize the outgoing response for the resource.
     *
     * @param  \Illuminate\Http\Request  $request
     * @param  \Illuminate\Http\Response  $response
     * @return void
     */
    public function withResponse($request, $response)
    {
        if (property_exists($this->resource, 'pagination') && is_object($this->resource->pagination)) {
            if (isset($this->resource->pagination->total)) {
                $response->header('X-Total-Count', (string) $this->resource->pagination->total);
            }
            if (isset($this->resource->pagination->page)) {
                $response->header('X-Page-Number', (string) $this->resource->pagination->page);
            }
        }
    }
}
{{/model}}
{{/models}}
