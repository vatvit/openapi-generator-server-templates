<?php

declare(strict_types=1);

/**
 * {{appName}}
 * {{#appDescription}}
 * {{{appDescription}}}
 * {{/appDescription}}
 * PHP version {{phpVersion}}
 *
 * @category Class
 * @package  {{invokerPackage}}\Http\Controllers
 * @author   OpenAPI Generator
 * @link     https://openapi-generator.tech
 */

/**
 * NOTE: This class is auto-generated by OpenAPI Generator
 * https://openapi-generator.tech
 * Do not edit this class manually.
 */

namespace {{invokerPackage}}\Http\Controllers;

use Illuminate\Http\JsonResponse;
use Illuminate\Http\Resources\Json\JsonResource;
use {{apiPackage}}\{{classname}};
use {{invokerPackage}}\Http\Requests\{{classname}}Request;
{{#operations}}

/**
 * {{classname}}Controller
 *
 * HTTP Controller for {{baseName}} operations
 * Delegates business logic to injected handler interface
 *
 * CONTRACT ENFORCEMENT:
 * - Injects {{classname}} interface via constructor
 * - Validates all requests via {{classname}}Request (auto-generated from OpenAPI spec)
 * - Converts validated data to typed DTOs
 * - Delegates to interface with strict type enforcement
 * - Handler returns Resource (structure enforced)
 * - Controller maps Resource type to HTTP status code
 * - Developer MUST implement {{classname}} interface
 *
 * RESPONSE MAPPING:
 * - Handler returns JsonResource indicating response type
 * - Controller inspects Resource class and returns appropriate status code
 * - Response structure enforced by Resource toArray() method
 *
 * @category Controller
 * @package  {{invokerPackage}}\Http\Controllers
 * @author   OpenAPI Generator
 * @link     https://openapi-generator.tech
 */
class {{classname}}Controller
{
    /**
     * Constructor
     *
     * @param {{classname}} $handler Business logic handler implementing the API interface
     */
    public function __construct(
        private {{classname}} $handler
    ) {}

{{#operation}}
    /**
     * Operation {{operationId}}
     *
     * {{summary}}{{#notes}}
     * {{{notes}}}{{/notes}}
     *
     * @param {{classname}}Request $request Validated HTTP request (auto-validated by Laravel)
{{#pathParams}}
     * @param {{dataType}} ${{paramName}} {{description}}
{{/pathParams}}
     * @return JsonResponse HTTP JSON response
     */
    public function {{operationId}}({{classname}}Request $request{{#pathParams}}, ${{paramName}}{{/pathParams}}): JsonResponse
    {
        // Request is already validated by Laravel's FormRequest
        // If we reach here, all validation rules passed

{{#bodyParams}}
{{#isModel}}
        // Convert entire validated request body to typed DTO
        // The HTTP request body IS the {{dataType}} structure
        ${{paramName}} = \{{{dataType}}}::fromArray($request->validated());

{{/isModel}}
{{/bodyParams}}
{{#queryParams}}
        // Extract validated query parameter
        ${{paramName}} = $request->input('{{baseName}}'{{#defaultValue}}, {{{defaultValue}}}{{/defaultValue}});

{{/queryParams}}
        // Delegate to handler - returns Response Resource with HTTP code and headers enforced
        $resource = $this->handler->{{operationId}}({{#allParams}}${{paramName}}{{^-last}}, {{/-last}}{{/allParams}});

        // Response Resource sets its own status code and headers in withResponse()
        // Handler must return operation-specific Resource from {{invokerPackage}}\Http\Resources\Responses\
        return $resource->response();
    }
{{^-last}}

{{/-last}}
{{/operation}}
}
{{/operations}}
